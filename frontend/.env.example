# Frontend API base path used by the browser.
VITE_API_BASE_URL=/api

# Backend origin used by Vite's dev proxy for /api routes.
VITE_BACKEND_ORIGIN=http://localhost:5001

# Optional helper key used by demo admin account provisioning in the UI.
VITE_ADMIN_INVITE_KEY=change_this_admin_invite_key
